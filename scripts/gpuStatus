#!/bin/bash
# Find the first card that actually has the gpu_busy_percent file
usage_file=$(ls /sys/class/drm/card*/device/gpu_busy_percent 2>/dev/null | head -n 1)

if [ -f "$usage_file" ]; then
    usage=$(cat "$usage_file")
    echo "$usage%"
else
    echo "N/A"
fi